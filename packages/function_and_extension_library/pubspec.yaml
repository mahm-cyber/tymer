name: function_and_extension_library
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  domain_models:
    path: ../domain_models
  form_fields:
    path: ../form_fields
  meta: ^1.9.1
  intl: any
  collection: ^1.18.0
dev_dependencies:
  flutter_lints: ^4.0.0
