name: monitoring
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_crashlytics: ^4.2.0
  firebase_analytics: ^11.3.6
  routemaster: ^1.0.1


dev_dependencies:
  flutter_lints: ^4.0.0
