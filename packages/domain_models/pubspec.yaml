name: domain_models
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter

  function_and_extension_library:
    path: ../function_and_extension_library
dev_dependencies:
  flutter_lints: ^4.0.0
